{
  "name": "@nexus/face-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexus/adapters": "0.1.0",
    "@nexus/decision": "0.1.0",
    "@nexus/memory": "0.1.0",
    "@nexus/shared": "0.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
